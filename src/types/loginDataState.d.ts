export type LoginDataState = {
  email: string;
  emailIsValid: boolean;
  password: string;
  passwordIsValid: boolean;
  formIsValid: boolean;
};

export type RegisterDataState = {
  name: string;
  nameIsValid: boolean;
  email: string;
  emailIsValid: boolean;
  password: string;
  passwordIsValid: boolean;
  formIsValid: boolean;
};

